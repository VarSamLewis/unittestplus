from setuptools import setup, find_packages

setup(
    name="better_test",
    version="0.1.0",
    description="Lightweight Python utility for function testing and profiling",
    author="Sam Lewis",
    packages=find_packages(),
    python_requires=">=3.7",
)
